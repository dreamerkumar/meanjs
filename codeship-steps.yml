- type: serial
  name: mymongodb
  steps:
  - service: mymongodb
  name: web
  steps:
  - service: web
    command: bash -c "cd Development && git clone https://github.com/dreamerkumar/meanjs.git && cd meanjs && npm install && bower install --allow-root && grunt build && grunt test"
